import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // Replace 'moral-curiosity-approach-avoid' with your exact repository name
  base: '/moral-curiosity/', 
  build: {
    outDir: 'dist',
  }
})